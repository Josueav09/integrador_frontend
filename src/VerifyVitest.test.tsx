import { describe, it, expect } from 'vitest'

describe('Prueba de Verificación de Vitest', () => {
  it('debe ejecutar una aserción matemática básica correctamente', () => {
    expect(1 + 1).toBe(2)
  })
})
